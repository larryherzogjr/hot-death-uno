# Production web dependencies, mirrored from pyproject.toml. The rules modules
# themselves remain stdlib-only. Use constraints.txt for exact transitives.
fastapi==0.138.2
uvicorn[standard]==0.49.0
# Google OAuth sign-in (optional at runtime, but importable in every deployment).
# Authlib uses httpx; itsdangerous backs Starlette's signed session cookie.
authlib==1.7.2
itsdangerous==2.2.0
httpx==0.28.1
