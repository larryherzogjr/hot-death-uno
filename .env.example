# Copy to `.env` (same dir as docker-compose.yml). The `.env` file is gitignored.

# Shared passcode to gate game creation. Empty / unset = open server.
HDU_PASSCODE=

# Seconds between AI moves (how slowly the AI plays). Default 1.2.
HDU_AI_DELAY=1.2
