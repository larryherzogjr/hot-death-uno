# Copy to `.env` (same dir as docker-compose.yml) and set a shared passcode to
# gate game creation. Leave empty / delete the file for an open, no-passcode
# server. The `.env` file is gitignored.
HDU_PASSCODE=
